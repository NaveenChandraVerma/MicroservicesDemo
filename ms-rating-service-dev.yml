
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

  info:
    env:
      enabled: true
info:
  application:
    name: ms-rating-service
    desc: hello! this is rating microservice.
